class Point {
  public int x;
  public int y;
  
  public Point(int x, int y) {
    this.x= x;
    this.y= y;
  }
  
  public void draw(int radius, color c) {
    ellipseMode(RADIUS);
    fill(c);
    noStroke();
    ellipse(x, y, radius, radius);
  }
}
